[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
pythonpath = .
addopts = -v --tb=short
markers =
    pure: Tests that need no mocking (pure logic functions)
    mocked: Tests that mock Supabase or other external services
    slow: Tests that take more than a few seconds (synthetic model fitting)
